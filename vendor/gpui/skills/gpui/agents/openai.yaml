interface:
  display_name: "GPUI"
  short_description: "Build and review GPUI code"
  default_prompt: "Use $gpui to update GPUI code, docs, or examples with the current API."
policy:
  allow_implicit_invocation: true
