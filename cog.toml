from_latest_tag = false
ignore_merge_commits = true

[commit_types.ci]
omit_from_changelog = true

[changelog]
path = "CHANGELOG.md"

[git_hooks.commit-msg]
script = "cog verify --file $1 && cog check"
