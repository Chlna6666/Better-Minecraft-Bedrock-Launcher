name: Local Bedrock compatibility cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Compatibility cleanup is executed by the default-branch PR workflow.'
